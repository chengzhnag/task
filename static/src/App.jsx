import React, { useState, useEffect, useRef } from 'react';
import {
  message, Typography,
  Button, Spin,
  Dropdown, Card, Modal,
  Skeleton, Col, Row,
  Divider, Input,
  Space, Result,
} from 'antd';
import { MenuOutlined, SettingOutlined, DesktopOutlined, LeftOutlined, ReadOutlined } from '@ant-design/icons';
const { Title, Paragraph, Text, Link } = Typography;
const { confirm } = Modal;

function App() {

  return (
    <div style={{ padding: '6px 8px', minHeight: "100vh", background: "#eee", boxSizing: "border-box" }}>
      2222
    </div>
  );
}

export default App;